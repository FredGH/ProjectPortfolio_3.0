fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.20
streamlit==1.39.0
pydantic-settings==2.15.0
sqlalchemy==2.0.36
alembic==1.13.3
psycopg[binary]==3.2.3
# httpx stays below 0.28: anthropic 0.39.0 passes `proxies=` to httpx.Client,
# which 0.28 removed, so anthropic.Anthropic(...) raises TypeError and every image
# that installs this file could not build an Anthropic adapter. 0.27.2 is the
# version the test suite runs against. Raise both together.
httpx==0.27.2
anthropic==0.39.0
pyyaml==6.0.2
python-ulid==1.1.0
rapidfuzz==3.14.5
dlt[postgres]==1.5.0
fsspec==2024.10.0
docling==2.126.0
# dbt is deliberately NOT here: dbt-core 1.11 needs protobuf>=6 while
# streamlit 1.39 needs protobuf<6, so the two cannot share one environment
# (and every image installs this file). See requirements-dbt.txt.
coverage==7.6.4
pre-commit==4.0.1
ruff==0.8.0
black==24.10.0
isort==5.13.2
mypy==1.13.0
