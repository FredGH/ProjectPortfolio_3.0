name: tca CI

on:
  push:
    branches: ["**"]
    paths:
      - "tca/**"
      - ".github/workflows/ci-tca.yml"
  pull_request:
    branches: [main]
    paths:
      - "tca/**"
      - ".github/workflows/ci-tca.yml"
  workflow_dispatch:

env:
  PYTHON_VERSION: "3.11"
  NODE_VERSION: "20"
  POSTGRES_USER: tca_user
  POSTGRES_PASSWORD: tca_password
  POSTGRES_DB: tca_db
  DATABASE_URL: postgresql://tca_user:tca_password@localhost:5432/tca_db
  REDIS_URL: redis://localhost:6379/0
  TEST_ADMIN_CLIENT_ID: admin_01
  TEST_ADMIN_SECRET: changeme
  TEST_CLIENT_CLIENT_ID: client_cp_a
  TEST_CLIENT_SECRET: changeme

jobs:
  # ── Code quality ─────────────────────────────────────────────────────────
  quality:
    name: Code quality
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: tca
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install linters
        run: pip install "black==24.4.2" "isort==5.13.2" "ruff==0.4.4"

      - name: ruff
        run: ruff check --output-format=github .

      - name: isort
        run: |
          OUT=$(isort --check --diff . 2>&1) || {
            echo "$OUT"
            printf '\n### isort diff\n```diff\n%s\n```\n' "$OUT" >> "$GITHUB_STEP_SUMMARY"
            exit 1
          }

      - name: black
        run: |
          OUT=$(black --check --diff . 2>&1) || {
            echo "$OUT"
            printf '\n### black diff\n```diff\n%s\n```\n' "$OUT" >> "$GITHUB_STEP_SUMMARY"
            exit 1
          }

  # ── Python unit + integration tests ──────────────────────────────────────
  test-python:
    name: Python tests
    runs-on: ubuntu-latest
    needs: [quality]
    defaults:
      run:
        working-directory: tca

    services:
      postgres:
        image: timescale/timescaledb:latest-pg16
        env:
          POSTGRES_USER: tca_user
          POSTGRES_PASSWORD: tca_password
          POSTGRES_DB: tca_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U tca_user -d tca_db"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt coverage && pip install -e .

      - name: Install postgresql-client
        run: sudo apt-get install -y postgresql-client

      - name: Apply database schema
        run: PGPASSWORD=$POSTGRES_PASSWORD psql -h localhost -U $POSTGRES_USER -d $POSTGRES_DB -f init.sql

      - name: Seed synthetic data
        run: python -m ingestion.seed

      - name: Run tests
        run: coverage run -m unittest discover -s tests

      - name: Coverage report
        run: coverage report -m

  # ── dbt Data Vault build + tests ─────────────────────────────────────────
  test-dbt:
    name: dbt build
    runs-on: ubuntu-latest
    needs: [test-python]
    defaults:
      run:
        working-directory: tca

    services:
      postgres:
        image: timescale/timescaledb:latest-pg16
        env:
          POSTGRES_USER: tca_user
          POSTGRES_PASSWORD: tca_password
          POSTGRES_DB: tca_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U tca_user -d tca_db"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt && pip install -e .

      - name: Install postgresql-client
        run: sudo apt-get install -y postgresql-client

      - name: Apply database schema
        run: PGPASSWORD=$POSTGRES_PASSWORD psql -h localhost -U $POSTGRES_USER -d $POSTGRES_DB -f init.sql

      - name: Seed synthetic data
        run: python -m ingestion.seed

      - name: dbt deps
        run: dbt deps

      - name: dbt build (staging → raw_vault → biz_vault → marts)
        run: dbt build --target ci

      - name: dbt source freshness
        run: dbt source freshness --target ci

  # ── Angular production build ──────────────────────────────────────────────
  test-angular:
    name: Angular build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: tca/frontend/package-lock.json

      - name: Install dependencies
        working-directory: tca/frontend
        run: npm ci

      - name: Production build
        working-directory: tca/frontend
        run: npm run build -- --configuration production
