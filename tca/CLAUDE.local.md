# CLAUDE.local.md

Personal overrides — gitignored, not shared with the team.

## My Local Setup

- Python path: (your local python path)
- Virtual env: ./venv

## Personal Preferences

- I prefer concise responses without trailing summaries
- Show file references as clickable markdown links

## Local Secrets / Environment

Do not commit secrets here. Reference them by env var name only.

- `DATABASE_URL` — local Postgres connection
- `API_KEY` — service API key (see `.env`)

## Overrides

Any team conventions I want to locally override go here.
